{
  "name": "rubiks-cube-mosaic-maker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "wrangler": "^4.4.0"
  }
}
